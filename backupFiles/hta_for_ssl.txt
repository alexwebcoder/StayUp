
### HTTPS REDIRECT CODE START
### TEMPORARILY DISABLED FOR LET'S ENCRYPT VERIFICATION
# RewriteEngine On
# RewriteCond %{HTTPS} !=on
# RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301,NE]
Header always set Content-Security-Policy "upgrade-insecure-requests;"
### HTTPS REDIRECT CODE END

### CANONICAL HTTPS + NON-WWW REDIRECT
# RewriteEngine On

# RewriteCond %{HTTPS} !=on [OR]
# RewriteCond %{HTTP_HOST} ^www\.stayup\.com$ [NC]
# RewriteRule ^(.*)$ https://stayup.com/$1 [L,R=301]

Header always set Content-Security-Policy "upgrade-insecure-requests;"
